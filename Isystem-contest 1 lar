#include <bits/stdc++.h>

using namespace std;

int main()
{
  	long long int n, m=0, b;
  	cin >> n;
	
	for(int i=1; i <= n; ++i)
	{
		while(n>0){
		if(n%2==1) m++;
		n/=2; 
		{	
			i++;
		}
		}
	}
		cout <<m;
	
  return 0;
}
